mutation importExport(
  $portpholioId: BigInt!
  $name: String!
  $jsonData: [ProcessExportInput!]!
) {
  importExport(portpholioId: $portpholioId, name: $name, jsonData: $jsonData) {
    id
    name
  }
}
